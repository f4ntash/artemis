# Theme

Source tokens from `styles/globals.css`:

```css
:root {
  --bg:#f6f6f3;
  --surface:#ecece8;
  --fg:#181918;
  --muted:#92938d;
  --border:#d5d5cf;
  --accent:#20211f;
  --accent-secondary:#666762;
  --font-display:"Helvetica Neue",Helvetica,Arial,system-ui,sans-serif;
  --font-body:"Helvetica Neue",Helvetica,Arial,system-ui,sans-serif;
  --ease:cubic-bezier(.22,1,.36,1);
  --gutter:clamp(32px,5vw,88px);
}
```

Current visual language: warm off-white backgrounds, graphite typography, square geometry, Helvetica Neue, fine rules, editorial spacing, restrained uppercase metadata. Existing home uses no Tailwind and must remain unchanged.

Cinematic experiment extension: warm graphite `#0b0c0b`, limestone `#c8c2b6`, oxidized metal `#4b4a45`, low amber practical light `#d49a54`, soft bone text `#e7e3da`. No pure black, neon, glassmorphism, rounded cards, or gradients used as decorative identity.
